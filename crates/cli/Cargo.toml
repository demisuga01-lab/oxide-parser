[package]
name = "oxide-cli"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "oxide"
path = "src/main.rs"

[dependencies]
oxide-engine = { path = "../engine" }
clap = { version = "4", features = ["derive"] }
rayon = "1"
serde_json = "1"
zip = "2"
