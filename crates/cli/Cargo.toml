[package]
name = "oxide-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "oxide"
path = "src/main.rs"

[dependencies]
oxide-engine = { path = "../engine" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
zip = "2"
